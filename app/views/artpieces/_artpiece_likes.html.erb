<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Likes

    <a data-toggle="collapse" href="#artpiece_likes_new_form">
      Add like
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @artpiece.likes.each do |like| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to like, like %>

        <% if current_user == like.liker %>
          <%= link_to like, method: :delete do %>
            <i class="fas fa-trash-alt"></i>
          <% end %>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="artpiece_likes_new_form">
        <%= render partial: "artpieces/artpiece_likes_new_form" %>
      </div>
    </div>

  </ul>
</div>
