<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @artist, partial: "card", as: :artist %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "artists/artist_artpieces" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "artists/artist_exhibitions" %>
  </div>
</div>
  <!-- Show Page Customization end -->
